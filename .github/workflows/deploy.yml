name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]     # dispara em push na main
  workflow_dispatch: {}     # permite rodar manualmente

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Set BASE_PATH for GitHub Pages
        run: |
          if [ -n "atualizar_dashboard" ]; then
            echo "BASE_PATH=/atualizar_dashboard/" >> $GITHUB_ENV
          else
            echo "BASE_PATH=/${GITHUB_REPOSITORY#*/}/" >> $GITHUB_ENV
          fi

      - name: Create .env
        run: |
          echo "VITE_N8N_WEBHOOK=${{ secrets.VITE_N8N_WEBHOOK }}" > .env
          echo "VITE_N8N_WEBHOOK_RETAIL=${{ secrets.VITE_N8N_WEBHOOK_RETAIL != '' && secrets.VITE_N8N_WEBHOOK_RETAIL || secrets.VITE_N8N_WEBHOOK }}" >> .env
          echo "VITE_N8N_WEBHOOK_AUTO=${{ secrets.VITE_N8N_WEBHOOK_AUTO }}" >> .env
          echo "VITE_N8N_WEBHOOK_AUTO_MOVE=${{ secrets.VITE_N8N_WEBHOOK_AUTO_MOVE }}" >> .env

      - run: npm run build

      - name: SPA fallback
        run: cp dist/index.html dist/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
